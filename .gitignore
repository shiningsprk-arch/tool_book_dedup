# 打包产物：**仓库只放源码**，zip 发在 GitHub Releases（tag v<版本>，资源是
# <tool_id>-<版本>.zip）。这里保持忽略 —— 它由 scripts/build.py 从源码重建，
# 且从源码重打与 Release 里那份是**逐字节一致**的。
dist/
# 本地浏览器预览（scripts/make_preview.py 生成）
dev/

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/
*.swp
