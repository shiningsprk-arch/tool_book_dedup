# dist/ **入库**：zip 是要分发的东西，从仓库就能下载到"当时验过的那个包"
# （`dist/README.md` 写了怎么自己核对 sha256；从源码重打是逐字节一致的）。
# 本地浏览器预览不入库。
dev/

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/
*.swp